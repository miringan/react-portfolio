import { Col, Container, Row, Image } from "react-bootstrap";

const styles = {
  footerStyle:{
    position:"relative",
    left:0,
    bottom:0,
    right:0,
  }
}

const Footer = () => {
    return (
        <footer>
            <>
            
            </>
        </footer>
    )
}

export default Footer;